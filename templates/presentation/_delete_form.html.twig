<form method="post" action="{{ path('app_presentation_delete', {'id': presentation.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ? Attention cette action est irréverssible !');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presentation.id) }}">
    <button class="btn white">Supprimer</button>
</form>
