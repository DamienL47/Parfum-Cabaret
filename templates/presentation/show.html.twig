{% extends 'home.html.twig' %}

{% block title %}Presentation{% endblock %}

{% block body %}
    {% block texte %}
        <h1>Presentation</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ presentation.id }}</td>
                </tr>
                <tr>
                    <th>Text</th>
                    <td>{{ presentation.text }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_presentation_index') }}">retour à la liste</a>

        <a href="{{ path('app_presentation_edit', {'id': presentation.id}) }}">modifier</a>

        {{ include('presentation/_delete_form.html.twig') }}
    {% endblock %}
{% endblock %}
