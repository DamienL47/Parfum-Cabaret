{{ form_start(form) }}
    {{ form_label(form)}}
    {{ form_widget(form, {'attr': {'size': '1000'}})}}
    {{form_errors(form)}}
    <button class="btn white">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
